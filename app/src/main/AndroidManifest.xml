<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thebetadeveloper.app.first_avenue_customer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".homepage"></activity>
        <activity android:name=".staff_login" />
        <activity android:name=".mng_login" />
        <activity android:name=".staff_reset_pswd" />
        <activity android:name=".staff_dashboard" />
        <activity android:name=".mng_dashboard" />
        <activity
            android:name=".splah_screen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".staff_new_order"></activity>
    </application>

</manifest>